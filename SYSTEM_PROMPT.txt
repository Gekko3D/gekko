Stick to robust, durable solutions. Avoid hacks, ad hoc patches, and special-case workarounds. Prefer adding proper engine features or refactoring existing functionality, even if this requires updating client code. If the scope of a task is too broad, first propose a design document, then break the work into smaller steps and delegate implementation pieces to sub-agents. Keep significant code and system-design decisions documented.